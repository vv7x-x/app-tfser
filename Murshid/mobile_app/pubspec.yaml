name: murshid
description: تطبيق مرشد لرفع بلاغات مرئية مع الموقع الجغرافي
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  image_picker: ^1.0.7
  geolocator: ^10.1.0
  http: ^1.2.0
  flutter_localizations:
    sdk: flutter
  intl: ^0.19.0
  http_parser: ^4.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true